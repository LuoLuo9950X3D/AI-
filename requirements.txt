# 基础依赖
numpy>=1.19.0
pandas>=1.1.0
matplotlib>=3.3.0

# 深度学习框架
# 可以选择TensorFlow或PyTorch
# 推荐使用TensorFlow，因为我们的模型基于TensorFlow实现
# 如果要使用PyTorch，需要对model.py进行相应修改
tensorflow>=2.4.0
# torch>=1.7.0
# torchvision>=0.8.0

# CUDA支持（如果需要GPU加速）
# tensorflow-gpu>=2.4.0  # 如果使用独立的GPU版本

# 图形界面
pygame>=2.0.0

# 数据处理和可视化
scikit-learn>=0.24.0
seaborn>=0.11.0

# 工具库
tqdm>=4.50.0  # 进度条显示
# pickle5>=0.0.11  # 高效数据存储 (在Windows上需要Visual C++编译环境，Python 3.8+已自带类似功能)

# 可选依赖（用于高级功能）
# wandb>=0.10.0  # 实验跟踪和可视化
# optuna>=2.6.0  # 超参数优化

# 注意：根据您的操作系统和CUDA版本，可能需要安装特定版本的依赖
# 例如，在Windows上使用CUDA 11.0，推荐安装：
# tensorflow==2.4.1
# 详细版本兼容性请参考TensorFlow官方文档